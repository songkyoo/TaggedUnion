<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>Macaron.TaggedUnion.JsonSerializer</AssemblyName>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <RootNamespace>Macaron.Union</RootNamespace>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Version>$(TaggedUnionJsonSerializerVersion)</Version>
  </PropertyGroup>

  <PropertyGroup>
    <Authors>Gyu Song</Authors>
    <PackageId>Macaron.TaggedUnion.JsonSerializer</PackageId>
    <PackageLicenseExpression>Unlicense</PackageLicenseExpression>
    <RepositoryType>git</RepositoryType>
    <RepositoryUrl>https://github.com/songkyoo/TaggedUnion</RepositoryUrl>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Text.Json" Version="8.0.6" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\TaggedUnion\TaggedUnion.csproj" PrivateAssets="none" />
    <ProjectReference Include="..\TaggedUnion.JsonSerializer.Generator\TaggedUnion.JsonSerializer.Generator.csproj" PrivateAssets="all" ReferenceOutputAssembly="false" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\TaggedUnion.JsonSerializer.Generator\bin\$(Configuration)\netstandard2.0\Macaron.TaggedUnion.JsonSerializer.Generator.dll" Pack="true" PackagePath="analyzers/dotnet/cs" />
  </ItemGroup>

</Project>
